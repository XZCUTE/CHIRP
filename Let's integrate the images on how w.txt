Let's integrate the images on how will it work for the website such as for avatars, display picture, post picturs/gif and any feature using pictures use this

🧩 HOW TO USE ImgBB FOR CHIRP (React.js)
✅ STEP 1: Get Your ImgBB API Key

Go to 👉 

Sign in / Sign up

Copy your API Key

⚠️ Keep this key private (don’t hard-code it publicly if possible).

✅ STEP 2: Image Upload Flow (Concept)

User selects image / GIF

React sends the image to ImgBB

ImgBB returns an image URL

Save the URL to:

Firebase Realtime Database

Display image in post (CapyHome)

✅ STEP 3: Upload Image from React (Example)
📁 Image Upload Function
const uploadToImgBB = async (imageFile) => {
  const formData = new FormData();
  formData.append("image", imageFile);

  const response = await fetch(
    "",
    {
      method: "POST",
      body: formData
    }
  );

  const data = await response.json();
  return data.data.url; // image link
};

✅ STEP 4: Use the URL in a Post (Chirp)
const handlePost = async () => {
  const imageUrl = await uploadToImgBB(selectedImage);

  // Save to Firebase
  await set(ref(db, "posts/"), {
    text: postText,
    image: imageUrl,
    user: currentUser,
    timestamp: Date.now()
  });
};

✅ STEP 5: Display Image in Feed
{post.image && (
  <img
    src={post.image}
    alt="Chirp Image"
    style={{ maxWidth: "100%", borderRadius: "12px" }}
  />
)}

🔐 SECURITY BEST PRACTICES (IMPORTANT)
✔ File Validation

Before upload:

Allow only:

.jpg .png .gif .webp

Max file size (example: 5MB)

if (file.size > 5 * 1024 * 1024) {
  alert("File too large!");
  return;
}

📌 Where ImgBB Fits in CHIRP
Feature	Use ImgBB
Post images	✅
GIFs	✅
Profile pictures	✅
Media previews	✅




HARDCODE MY API KEY FOR IT BUT DON'T REVEAL IT. 

API KEY - a8d2263996f062376aa8341d5324956c
